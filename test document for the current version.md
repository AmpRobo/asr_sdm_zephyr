# 板卡测试与问题总结反馈

asr_sdm_zephyr

## 1. 测试结论

本轮实板测试范围内，软件代码及新增驱动适配工作正常，未发现需要归因于业务代码或驱动逻辑的功能性问题。已完成以下软件工作：

1. 新增 ICM-45686 IMU 驱动适配；
2. 升级 CAN 通信协议适配；
3. 新增 HP206F 气压计驱动；
4. 完成上述功能的板级联调和通信验证。

**当前主要问题集中在硬件外围电路和系统接线设计：**

- **HP206F 的 I²C 总线未配置外部上拉电阻，当前依赖 MCU 软件/内部上拉维持通信；**
- **隔离型 TTL-RS485 模块的隔离侧参考地未纳入标准线束，现场需额外连接 VO-、执行器电源负极和舵机 GND，才能为 A/B 差分信号提供稳定的共模参考。**

## 2. 软件与驱动测试结果

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| ICM-45686 IMU 适配 | 通过 | 已增加新 IMU 的驱动和板级适配，使用 SPI 接口。 |
| CAN 通信协议升级 | 通过 | 已完成新协议适配，CAN 通信功能在本轮测试中正常。 |
| HP206F 气压计驱动 | 通过（带硬件限制） | 驱动功能正常；因板上缺少 I²C 外部上拉，当前代码启用了软件/内部上拉作为临时措施。 |
| 原有代码功能 | 通过 | 本轮测试未发现代码侧功能性缺陷。 |

## 3. 硬件问题清单

| 编号 | 问题 | 影响 | 当前临时措施 | 优先级 |
| --- | --- | --- | --- | --- |
| HW-01 | HP206F 的 SDA/SCL 缺少外部上拉电阻 | 上电初始化、通信边沿和抗干扰能力依赖 MCU 内部弱上拉；不同温度、批次、线长或总线速率下可能出现不稳定 | 在代码中使能软件/内部上拉 | 高 |
| HW-02 | RS485 隔离侧 VO- 未纳入标准线束/接线定义 | A/B 仅两线连接时，舵机侧缺少明确参考地，共模电压可能漂移，导致丢帧、误码或无法通信 | 现场额外补接 VO- 与执行器电源负极/舵机 GND | 高 |

## 4. 问题分析与修改建议

### 4.1 HW-01：HP206F I²C 上拉电阻缺失

HP206F 的 SDA、SCL 为 I²C 信号。器件资料中的典型应用电路在 SDA 和 SCL 上分别配置 10 kΩ 上拉电阻，并上拉到传感器逻辑电源。当前原理图中气压计仅配置了电源去耦，未看到 SDA/SCL 外部上拉电阻。

**当前状态**

- 驱动通过 MCU GPIO 内部上拉实现通信；
- 可用于样机调试和短期验证；
- 不建议作为正式版硬件的长期方案。

**建议修改**

1. 在 HP206F 的 SDA 和 SCL 上分别增加外部上拉电阻到 3.3 V；
2. 首版建议按器件典型应用使用 10 kΩ；
3. 预留 4.7 kΩ～10 kΩ 的阻值调整空间，并根据总线电容、线长和目标速率验证上升时间；
4. 电阻应靠近总线主控或传感器所在区域布置，避免长支路；
5. 正式硬件验证通过后，代码中不再依赖内部上拉，可保留为防呆但不作为设计前提。

```
3V3 ── R_SCL（10 kΩ）── SCL
3V3 ── R_SDA（10 kΩ）── SDA
```

### 4.2 HW-02：RS485 隔离侧参考地接线不完整

所用 B13-4S3 为隔离型 TTL-RS485 模块。模块的 Vin-/Vin+ 属于控制侧供电，VO-/VO+ 属于隔离侧输出电源参考。RS485 的 A/B 是差分信号，但接收器仍有允许的共模范围；当舵机与通信模块之间只有 A/B 两根线、且两端没有参考地连接时，共模电位可能漂移。

当前通信板已经将 VO- 引出为 RS485_VO-，但系统线束若只接 A/B，则仍需现场增加一根参考地线。

**建议标准接线**

```
通信板 RS485_A   ───────── 舵机 A
通信板 RS485_B   ───────── 舵机 B
通信板 RS485_VO- ─────┬── 执行器/舵机电源 GND
                       └── 舵机通信参考 GND
```

**注意事项**

- 应连接的是隔离侧 VO- / RS485_GND 与舵机侧电源负极，而不是用控制器侧 Vin- / GND 替代；
- 若将隔离侧 VO- 直接与控制器侧 GND 短接，会旁路隔离，应根据系统接地策略谨慎处理；

## 5. 建议的硬件改版项

| 改版项 | 具体修改 | 验收方式 |
| --- | --- | --- |
| 气压计上拉 | HP206F 的 SDA、SCL 分别增加 10 kΩ 上拉电阻到 3.3 V，避免依赖 MCU 软件/内部上拉 | 关闭 MCU 内部上拉后，气压计仍可稳定初始化并连续读取数据 |
| 串口转 RS485 模块额外接线 | 增加接线，将模块 VO-、电源模块 GND 与舵机 GND 连接，为 A/B 差分信号提供参考地 | 按新增接线连接后，舵机通信稳定，无丢帧、误码或无法通信现象 |

## 6. 最终反馈结论

本轮测试表明，代码及新增驱动适配能够完成预期功能，当前无需以代码缺陷为方向继续排查。需要在下一版硬件和线束中优先修正以下两项：

1. 为 HP206F 的 SDA/SCL 增加外部上拉电阻；
2. 为串口转 RS485 模块增加额外接线，将 VO-、电源模块 GND 与舵机 GND 连接，为 A/B 差分信号提供参考地。
